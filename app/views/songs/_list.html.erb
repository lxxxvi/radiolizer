<% if songs.any? %>
  <ul class="songs">
      <% songs.each do |song| %>
        <li>
          <%= render partial: 'songs/item', locals: { song: song, display: display } %>
        </li>
      <% end %>
  </ul>
<% end %>